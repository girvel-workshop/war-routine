#!/usr/bin/bash

if [[ "$1" == "run" ]]; then
	love .
elif [[ "$1" == "commit" ]]; then
    git add .
    git commit -m "$2"
    git push origin master
elif [[ "$1" == "test" ]]; then
    busted tests/test.lua
fi